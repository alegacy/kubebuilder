---
apiVersion: v1
kind: Service
metadata:
  name: controller-manager-delve-service
  namespace: system
  labels:
    control-plane: controller-manager
    controller-tools.k8s.io: "1.0"
spec:
  type: NodePort
  selector:
    control-plane: controller-manager
    controller-tools.k8s.io: "1.0"
  ports:
    - name: delve
      port: 30000
      nodePort: 30000
